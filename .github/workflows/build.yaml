



name: Build
on: push


jobs:
    build:
        name: Build Application
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v2
            # TODO: Basic build action for ddev php project
